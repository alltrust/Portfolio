// import { IActions, IContextReducer } from '../types/theme-actions';
import { IContextInitialState } from '../types/context-types';
const reducer = (state:any, action: any)=>{

  switch(action.type){

  default:
    return state;

  }

};

export default reducer;
